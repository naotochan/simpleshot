import SwiftUI

@main
struct PashattSigningApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Pashatt signing helper — Team を選んで閉じてもOK")
                .padding(40)
        }
    }
}
