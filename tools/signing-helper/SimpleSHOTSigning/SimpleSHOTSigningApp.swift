import SwiftUI

@main
struct SimpleSHOTSigningApp: App {
    var body: some Scene {
        WindowGroup {
            Text("SimpleSHOT signing helper — Team を選んで閉じてもOK")
                .padding(40)
        }
    }
}
