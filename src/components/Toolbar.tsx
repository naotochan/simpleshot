/**
 * Re-export for backward-compatible imports.
 * Implementation lives in ./toolbar/
 */
export { default } from "./toolbar/Toolbar";
export type {
  Tool,
  AnnotationColor,
  ArrowStyle,
  ToolbarProps,
  ToolControls,
  BackgroundControlsState,
  BackgroundState,
  FrameControls,
  HistoryControls,
  CropControls,
  ColorControls,
} from "./toolbar/Toolbar";
